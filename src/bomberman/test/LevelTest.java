package bomberman.test;

import static org.junit.Assert.*;
import org.junit.Test;

//Because we are using the slick2D framework to do our game its difficult to do automatic Junit tests like we were asked
//So, instead of writing the code for the junit tests, we are commenting what was the primary function of each test

public class LevelTest {

	@Test
	public void testLevelEnd() {
		//This test is supposed to check if the level reaches its end when the monster killed all the monsters and found the exit
	}
	
	@Test
	public void testChangeLevel(){
		//This test is supposed to check if the level changes when the current level ends
	}

}
